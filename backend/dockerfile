# Use uma imagem base do Python
FROM python:3.9-slim-buster

# Defina o diretório de trabalho dentro do contêiner
WORKDIR /app

# Copie os arquivos de dependência
COPY requirements.txt /app/

# Instale as dependências
RUN pip install -r requirements.txt

# Instalar netcat para teste de conexão com o banco
RUN apt-get update && apt-get install -y netcat && rm -rf /var/lib/apt/lists/*

# Copie o código fonte do projeto (o conteúdo de 'backend' vai para '/app'
COPY . /app/

# Garantir permissões corretas e quebras de linha do entrypoint.sh
RUN chmod +x /app/entrypoint.sh && \
    sed -i 's/\r$//' /app/entrypoint.sh

# Expor a porta que o Django usa (padrão: 8000)
EXPOSE 8000

# Comando para executar o entrypoint script
CMD ["/app/entrypoint.sh"]
